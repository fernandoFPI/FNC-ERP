# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
*.tsbuildinfo
.turbo/

# graphify knowledge graph (regeneratable via `/graphify . --update`)
graphify-out/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Test coverage
coverage/

# Docker volumes (local)
postgres_data/
redis_data/

# User uploads (runtime files, not source code)
services/gateway/dev-uploads/

# Odoo source extraction (reference only, not part of the project)
odoo_deb_extracted/

# OneDrive sync-conflict copies (e.g. "app-DESKTOP-G9TJJ4A.ts") — never source
*-DESKTOP-*

# Deploy pipeline: marks the last commit SHA that passed post-deploy health checks
.last-good-deploy
